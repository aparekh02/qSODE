numpy>=1.24.0
torch>=2.0.0
torchdiffeq>=0.2.3
matplotlib>=3.7.0
scipy>=1.10.0
pillow>=9.5.0
tqdm>=4.65.0
pandas>=2.0.0
seaborn>=0.12.0

# Quantum Computing (Qiskit)
qiskit>=1.0.0
qiskit-aer>=0.13.0

# Optional for MP4 video output (GIF works without):
# ffmpeg (install via brew/apt, not pip)
